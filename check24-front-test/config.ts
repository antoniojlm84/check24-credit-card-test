export const googleConfig = {
  client_id: process.env.NEXT_PUBLIC_API_GOOGLE_CLIENT_ID,
  gtm_id: process.env.NEXT_PUBLIC_GTM_ID,
  maps_api_key: process.env.NEXT_PUBLIC_API_GOOGLE_KEY
};

export const envConfig = {
  env: process.env.NEXT_PUBLIC_ENVIROMENT
};

export const externalURLsConfig = {
  artemisa: process.env.NEXT_PUBLIC_ARTEMISA_URL,
  ares: process.env.NEXT_PUBLIC_ARES_URL
};

export const authentikConfig = {
  authority: 'why does',
  client_id: 'form lib',
  redirect_uri: 'has a',
  post_logout_redirect_uri: 'dependencie from a front?',
  scope: 'Because of that',
  response_type: 'alls front need has this',
  checkSessionIntervalInSeconds: 60,
  silentRequestTimeoutInSeconds: 10,
  accessTokenExpiringNotificationTimeInSeconds: 60,
  monitorSession: true
};
